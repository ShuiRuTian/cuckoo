//! `cuckoo-ws`：WebSocket 客户端（`tokio-tungstenite` 封装）。
//!
//! M0 阶段仅占位，具体实现见 `plan.md` M4 / `spec.md` 5.2 节：
//! 主动连接/发送帧/接收帧的 Service 方法，逐帧事件通过 SSE 推送给前端。

#[allow(dead_code)]
const _PLACEHOLDER: () = ();
